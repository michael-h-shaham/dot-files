# Michael Shaham's dot files

To use:

```bash
cd ~/Documents/ && git clone git@github.com:michael-shaham/dot-files.git
cd dot-files
cp bashrc ~/.bashrc
cp tmux.conf ~/.tmux.conf
cp vimrc ~/.vimrc

# if using vscode on system:
cp vscode_settings.json ~/.config/Code/User/settings.json
```